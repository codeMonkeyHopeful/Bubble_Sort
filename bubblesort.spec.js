describe('Bubble Sort', function(){
    it('handles an empty array', function(){
      expect( bubbleSort([]) ).toEqual( [] );
    });
  });

describe('Bubble Sort', function(){
  it('handles an empty array', function(){
    expect( bubbleSort([3,1,2]) ).toEqual( [1,2,3] );
  });
});

describe('Bubble Sort', function(){
  it('handles an empty array', function(){
    expect( bubbleSort([3,1,2,4]) ).toEqual( [1,2,3,4] );
  });
});

describe('Bubble Sort', function(){
    it('handles an empty array', function(){
      expect( bubbleSort(['c','b','a']) ).toEqual( ['a','b','c'] );
    });
  });

  describe('Bubble Sort', function(){
    it('handles an empty array', function(){
      expect( bubbleSort(['all','bat','A']) ).toEqual( ['a','b','c'] );
    });
  });



// describe('Bubble Sort', function (){
//   it('getting to the center of tootsiepop involves exactly three licks', function() {
//     expect(sort.calls.count()).toEqual(0);
//   });
// });